spring.application.name=backend
# ===============================
# SERVER
# ===============================
server.port=8080

# ===============================
# DATABASE (MySQL)
# ===============================

spring.datasource.url=jdbc:mariadb://localhost:3306/mpps
spring.datasource.username=root
spring.datasource.password=Pass123!
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.servlet.context-path=/api

# ===============================
# JWT Configuration
# ===============================
jwt.secret=fgsdfg45sg8srgefyuegf7b548h516aret4hd6f6sg461gfhdf
jwt.expiration=86400000

# ===============================
# CORS Configuration
# ===============================
# Allowed origins (comma-separated)
# For localhost: http://localhost:5173,http://localhost:3000
# For ngrok: add your ngrok URL here, e.g. https://abc123.ngrok.io
# You can add multiple origins separated by commas
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://uneroded-unpreponderating-leida.ngrok-free.dev