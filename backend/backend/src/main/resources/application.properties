spring.application.name=backend
# ===============================
# SERVER
# ===============================


# ===============================
# DATABASE (MySQL)
# ===============================

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}
# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.servlet.context-path=/api


RAZORPAY_KEY=rzp_test_SNCRhEiGrpeoUG
RAZORPAY_SECRET=ZCYHCupxMlxuZCRnWIoA1ggJ
# ===============================
# REDIS / CACHE
# ===============================
spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# ===============================
# JWT Configuration
# ===============================
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# ===============================
# PASSWORD HASHING
# ===============================
# Keep bcrypt adaptive cost >= 10 in production.
app.security.password.bcrypt-strength=${APP_SECURITY_PASSWORD_BCRYPT_STRENGTH:10}
# Limits in-flight bcrypt operations to smooth burst traffic (login/register).
app.security.password.max-concurrent-hashes=${APP_SECURITY_PASSWORD_MAX_CONCURRENT_HASHES:8}

# ===============================
# CORS Configuration
# ===============================

app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://uneroded-unpreponderating-leida.ngrok-free.dev,https://192.168.101.220