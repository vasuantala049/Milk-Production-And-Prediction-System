package com.example.backend.Entity.type;

public enum AllocationType {
    ORDER,          // One-time purchase order
    SUBSCRIPTION,   // Recurring subscription allocation
    RESERVATION     // Reserved for future use
}
